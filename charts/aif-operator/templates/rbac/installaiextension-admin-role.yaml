{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "aif-operator.labels" . | nindent 4 }}
  name: {{ include "aif-operator.serviceName" (dict "context" . "suffix" "installaiextension-admin") }}
rules:
  - apiGroups:
      - ai-factory.suse.com
    resources:
      - installaiextensions
    verbs:
      - '*'
  - apiGroups:
      - ai-factory.suse.com
    resources:
      - installaiextensions/status
    verbs:
      - get
{{- end }}
